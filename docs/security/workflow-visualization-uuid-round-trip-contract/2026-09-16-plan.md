# Plano de implementação — Workflow Visualization UUID Round-trip Contract

Plano revisável exigido pelo §6.3 do Controlled Engineering Workflow em `f60cf9a56679d4d7083b2c11ac4e2727d53d84c3`, derivado do desenho aprovado em `2026-09-16-design.md` deste mesmo diretório. Nenhum identificador numérico de unidade é criado.

- Base autorizada: `d70bf5a59ae8d3ce89dc0c84b6b9fe9315dd59e7`, branch `codex/g2-override-logs-20260915`, worktree `/private/tmp/structr-g2-override-logs-20260915`. `d005913b` e `d70bf5a5` permanecem imutáveis.
- Este plano **não autoriza implementação**. Descreve ciclos verificáveis, evidências exigidas e fronteiras de commit, para aprovação humana.
- Ambiente limpo em toda execução: `env -i PATH=/usr/local/bin:/usr/bin:/bin NODE_ENV=test CI=1`.
- Artefato documental de evidência desta unidade, **futuro e não criado nesta rodada**: `docs/security/workflow-visualization-uuid-round-trip-contract/2026-09-16-evidence.md`.

## 0. Fatos medidos que condicionam o plano

**0.1 O Vitest transpila sem checar tipos.** Uma fixture `id: 1` tipada como `WorkflowVisualizationData` não falharia na execução dos testes depois de o tipo virar `string`. O `pnpm check` padrão cobre `server/**` e `client/src/**`, mas **exclui** `**/*.test.ts`. Sem verificação de tipos dedicada há falso verde.

**0.2 `sprint17-workflow-viz.test.ts` já tem muitos diagnósticos anteriores a esta unidade.** Medição na base `d70bf5a5` com `tsconfig` de evidência estendendo o do repositório:

| Arquivo | Diagnósticos na base |
|---|---|
| `server/sprint17-workflow-viz.test.ts` | **56** erros (TS2322 em maioria; também TS2367, TS2769, TS2345, TS7053) |
| `server/tenant-g2-rule-read-callers.test.ts` | 0 |
| `server/tenant-g2-override-log-callers.test.ts` | 0 |

Portanto **não se exige "zero erros nos quatro arquivos"**; a estratégia correta está na seção 4.

**0.3 Nota operacional resolvida pela escolha do caminho.** O `tsconfig` de evidência precisa resolver `types: ["node", "vite/client"]`. Um config colocado **fora** da árvore do repositório falha com **TS2688** antes de emitir qualquer diagnóstico útil, porque o `typeRoots` padrão não encontra `node_modules` — isso foi observado na medição da base. O caminho adotado na seção 4 fica **dentro** do repositório, sob `.superpowers/`, que o `.gitignore` já exclui do versionamento: a resolução de tipos funciona e o arquivo continua fora dos commits.

**0.4 Construtos numéricos existentes nas duas suítes G2** (medidos, com linha exata):

| Arquivo | Posição | Construto |
|---|---|---|
| `tenant-g2-rule-read-callers.test.ts` | `:152` | `loadVisualization({ scopeDraftId: 17 })` |
| | `:319` | `const input = { scopeDraftId: 17, tenantId: TENANT_B, userId: "spoofed" }` |
| | `:188` | `store.scope_drafts = [{ ...draft }, { ...draft, id: "17" }]` |
| | `:9` | comentário: "Visualization keeps its existing numeric input solely to exercise the handoff" |
| | `:318` | nome do teste: "…through its existing numeric-input boundary" |
| `tenant-g2-override-log-callers.test.ts` | `:189` | `loadVisualization({ scopeDraftId: 17 })` |
| | `:303` | `loadVisualization({ scopeDraftId: 17 })` |
| | `:304` | asserção `toHaveBeenCalledWith(authority, which === "visualization" ? "17" : DRAFT, permission)` |
| | `:205` | `store.scope_drafts = [{ ...draft }, { ...draft, id: "17" }]` |

A linha `:37` do segundo arquivo (`store.scope_drafts.find(row => row.id === id)`) é o próprio dublê de driver e **permanece**.

## 1. Dois ciclos verificáveis

Cada ciclo tem RED próprio, que deve falhar **pelo motivo específico que o GREEN seguinte corrige**. Falha por import, tipo, assinatura ausente ou fixture vazia **não conta**. Se um RED não puder ser demonstrado pelo motivo esperado, **interromper e reportar**.

**Cada GREEN deixa a árvore inteira compatível**: nenhum ciclo termina com cliente ou suítes G2 desalinhados do contrato vigente.

### Ciclo A — contrato de entrada

| Item | Definição |
|---|---|
| **Arquivo e comportamento** | `server/workflow-visualization-uuid-contract.test.ts` (novo). As duas rotas aceitam UUID e recusam número |
| **RED A** | (A1) `loadVisualization` com UUID válido e contexto autorizado alcança o guard e retorna a visualização; (A2) `listDraftsForProject` com UUID de projeto autorizado retorna a lista; (A3) `17` em ambas as rotas recusa com `BAD_REQUEST` **sem** chamar guard, aquisição de banco ou helpers |
| **Comando exato** | `env -i PATH=/usr/local/bin:/usr/bin:/bin NODE_ENV=test CI=1 pnpm exec vitest run server/workflow-visualization-uuid-contract.test.ts --pool=forks --maxWorkers=1 --minWorkers=1` |
| **Resultado esperado** | exit 1, com A1, A2 e A3 falhando |
| **Razão válida da falha** | A1/A2: `z.number().int().positive()` (`:122`, `:315`) recusa a string UUID na validação. A3: `17` **atravessa** a validação e alcança guard/helpers. Outra razão invalida o RED |
| **Condição de parada** | A1/A2 falharem por fixture, mock ou import; A3 passar acidentalmente; razão observada diferente |
| **Evidência preservada** | Log completo do RED A com classificação por asserção, em `.superpowers/` (fora dos commits) |

**GREEN A — tudo que depende da entrada, de uma vez:**

1. `server/workflow-visualization-router.ts`: `:122` e `:315` passam a `z.string().uuid()`; remover as **cinco** coerções de entrada `:125`, `:128`, `:149`, `:318`, `:320`.
2. `client/src/pages/Workflow.tsx`: propriedade `:483` passa a `string`; remover `Number(...)` em `:571` e `:662`. A apresentação **não** muda neste ciclo.
3. `tenant-g2-rule-read-callers.test.ts`: `:152` e `:319` passam a `DRAFT`; remover o elemento sintético `{ ...draft, id: "17" }` de `:188`; atualizar o comentário `:9` e o nome do teste `:318`, que ainda dizem "numeric input".
4. `tenant-g2-override-log-callers.test.ts`: `:189` e `:303` passam a `DRAFT`; a asserção `:304` passa a esperar `DRAFT` (elimina o ramo `which === "visualization" ? "17" : DRAFT`); remover o elemento sintético de `:205`.

**Não** tocar no tipo de retorno, nas conversões de saída, na apresentação do cliente nem nas fixtures do `sprint17`. Ao fim: comando focal do Ciclo A em exit 0, e as duas suítes G2 verdes.

### Ciclo B — round-trip de saída

| Item | Definição |
|---|---|
| **Arquivo e comportamento** | Mesma suíte nova. Os identificadores devolvidos são exatamente os das linhas carregadas |
| **RED B** | (B1) `project.id` do retorno **estritamente igual** à string UUID da linha de projeto; (B2) `scopeDraft.id` **estritamente igual** à do rascunho |
| **Pré-condição** | GREEN A concluído; sem ele a chamada não alcança o handler e o RED B falharia pelo motivo errado |
| **Comando exato** | O mesmo comando focal da suíte nova |
| **Resultado esperado** | exit 1, com B1 e B2 falhando |
| **Razão válida da falha** | `Number(project.id)` (`:277`) e `Number(draft.id)` (`:284`) produzem `NaN`. Falha por indefinido, campo ausente ou erro de tipo **não** conta |
| **Condição de parada** | B1/B2 falharem por outro motivo, ou passarem antes do GREEN B |
| **Evidência preservada** | Log do RED B com o valor observado (`NaN`) ao lado do UUID esperado |

**GREEN B — tudo que depende da saída, de uma vez:**

1. `server/workflow-visualization-router.ts`: `:81` e `:88` passam a `string`; remover as **duas** coerções de identidade `:277` e `:284`. **Preservar** `Number(item.quantity)` `:350`, `Number(item.confidence)` `:354` e `Number(draft.confidence)` `:362`.
2. `client/src/pages/Workflow.tsx`: função local pura de apresentação usada **somente em JSX**, aplicada em `:514` e `:650`, no formato `Draft <8 caracteres>`, **sem `#`** e sem sugerir numeração. Invariante: `value={d.id}` (`:649`), estado (`:563-564`), chaves de cache e argumentos das consultas mantêm o **UUID completo**.
3. `server/sprint17-workflow-viz.test.ts`: fixtures de `WorkflowVisualizationData` em `:328-329` e `:352-353` passam a UUID. As asserções de existência, contagem e tipo (`:622-651`) permanecem intactas.

Cada alteração de fixture recebe nota **HISTORY**: é ajuste de tipo decorrente do contrato, **não** prova nova.

### REFACTOR

Somente após os dois GREEN: revisão de nomes, comentários e duplicação; reexecução de todas as provas, dos controles e da regressão completa. Nenhuma asserção pode ser enfraquecida para acomodar a implementação.

## 2. Casos da nova suíte

**Fronteira obrigatória:** `project-access` **real**; aquisição de banco **instrumentada** (registra chamadas e serve linhas sintéticas de existência); helpers comerciais de leitura (`scope-db`, `project-db`, `assembly-db`, `remodel-db`, `geo-override-db`) como dublês nomeados que registram se foram chamados. **É proibido substituir a decisão de autorização por um mock do próprio guard.** Nenhum PostgreSQL é exigido: esta unidade não altera SQL.

| Caso | Natureza | Oráculo |
|---|---|---|
| A1, A2 | RED A → GREEN A | UUID válido atravessa a validação e alcança guard/handler |
| A3 | RED A → GREEN A | `17` recusa com `BAD_REQUEST` sem guard, banco ou helpers |
| B1, B2 | RED B → GREEN B | `project.id` e `scopeDraft.id` idênticos às linhas carregadas |
| Q1 | Controle | `quantity` do retorno **permanece numérica**, protegendo `Number(item.quantity)` `:350` e as demais conversões de medida |
| F1 | Prova de fronteira | `listDraftsForProject` com caller autenticado e `tenantId = null` **ainda alcança** `requireProjectAccessTrpc` (aquisição instrumentada confirma) — distingue `protectedProcedure` de `tenantProcedure` e comprova que a fronteira não mudou |
| X1 | Isolamento, ver seção 5 | UUID estrangeiro alcança o guard real e recebe `FORBIDDEN` sem tocar helpers comerciais |

Casos negativos que enviam `17` ou `null` usam **cast explícito no ponto da chamada**, exclusivamente para exercitar a validação em runtime, de modo que a suíte termine com **zero erros TypeScript**.

## 3. Controles de preservação — não exigem falha no RED

Devem passar **antes e depois** de cada ciclo; sua aprovação na base **não** invalida nenhum RED:

- `""`, `"abc"` e `null` continuam recusados com `BAD_REQUEST`;
- **ausência de escrita** em todos os casos, inclusive nos que lançam, por observação direta do estado;
- **recusas já existentes**: `loadVisualization` sem empresa resolvida continua recusando por `tenantProcedure`;
- **fronteira de `listDraftsForProject`** permanece `protectedProcedure` com `requireProjectAccessTrpc` intacto (caso F1).

O isolamento entre empresas **não** entra aqui: tem tratamento próprio na seção 5, porque seu mecanismo muda com o Ciclo A.

## 4. Estratégia de tipos, por estágio

**Configuração de evidência — futura e não commitada:** `.superpowers/sdd/workflow-visualization-uuid-round-trip-contract/test-tsconfig.json`

```json
{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "incremental": false,
    "target": "ES2022",
    "noEmit": true
  },
  "exclude": [],
  "include": [
    "../../../server/workflow-visualization-uuid-contract.test.ts",
    "../../../server/sprint17-workflow-viz.test.ts",
    "../../../server/tenant-g2-rule-read-callers.test.ts",
    "../../../server/tenant-g2-override-log-callers.test.ts"
  ]
}
```

**Comando exato dos diagnósticos dos testes:**

```sh
env -i PATH=/usr/local/bin:/usr/bin:/bin NODE_ENV=test CI=1 pnpm exec tsc --project .superpowers/sdd/workflow-visualization-uuid-round-trip-contract/test-tsconfig.json --pretty false
```

**Comando exato para capturar separadamente os erros intermediários do cliente** (estágio 2), já que `client/src/**` é coberto pelo check da aplicação e não por este config:

```sh
env -i PATH=/usr/local/bin:/usr/bin:/bin NODE_ENV=test CI=1 pnpm check --incremental false
```

As saídas dos dois comandos são preservadas como evidência **fora dos commits**.

Comparação por **fingerprint normalizado** — `arquivo + código TS + texto da mensagem`, **sem linha e sem coluna** —, porque as notas HISTORY deslocam linhas e invalidariam uma comparação posicional.

| Estágio | Momento | Registro exigido |
|---|---|---|
| 1 | Base `d70bf5a5` | 56 diagnósticos legados em `sprint17`; **zero** nas duas suítes G2 |
| 2 | Após mudar **somente a entrada do servidor** | Erros esperados no cliente e nas duas suítes G2, registrados **antes** de adaptá-los |
| 3 | Após adaptar entrada (cliente + suítes G2) | **Zero** erros novos nesses arquivos |
| 4 | Após mudar **somente o tipo de saída** | Os **quatro** erros esperados nas fixtures de `WorkflowVisualizationData` do `sprint17` |
| 5 | Após adaptar as fixtures | **Nenhum diagnóstico novo** em relação à base, por fingerprint |

Se um estágio não produzir os diagnósticos previstos, a mudança não surtiu efeito: **parar e reportar**. **É proibido corrigir qualquer um dos 56 erros legados** de `sprint17` — fazê-lo é ampliação de escopo e exige unidade própria.

## 5. Isolamento entre empresas: mecanismo muda com o ciclo

O mecanismo da recusa **não é o mesmo antes e depois**, e o plano não pode afirmar o contrário:

- **Na base:** um UUID estrangeiro é recusado **pela validação de entrada, antes do guard** — a rota nunca chega a avaliar autorização. Registrar exatamente assim; **não** afirmar que o controle já passa pelo guard.
- **Após o GREEN A:** provar que o UUID estrangeiro **alcança o guard real** (`project-access`, com aquisição instrumentada confirmando a consulta) e recebe **`FORBIDDEN`**, **sem** alcançar os helpers comerciais de leitura.

Essa transição é ganho de cobertura da unidade e deve ser descrita como tal na evidência.

## 6. Contraste da corrupção de saída na base

Para que o defeito de saída tenha contraste na base, e não apenas depois do GREEN A:

- **Na base `d70bf5a5`**, pelo caminho numérico legado e com fixture controlada de existência (linha de rascunho cujo `id` satisfaz o guard, enquanto projeto e rascunho carregam **UUID** nos campos mapeados), registrar que as linhas com UUID chegam ao mapeamento e produzem **`NaN`** em `project.id` e `scopeDraft.id`.
- **Depois do GREEN A**, o **RED B definitivo** usa UUID de ponta a ponta e falha **especificamente** pelas duas conversões `Number(...)` (`:277`, `:284`).

O guard real permanece em vigor nos dois registros. **Se não for possível obter esse contraste sem enfraquecer o guard real, parar e reportar** — não substituir o guard por mock para produzir o contraste.

## 7. Verificação completa

| Verificação | Comando | Critério |
|---|---|---|
| Tipos da aplicação | `pnpm check --incremental false` | exit 0, zero erros (cobre router e cliente) |
| Tipos dos testes | `pnpm exec tsc --project .superpowers/sdd/workflow-visualization-uuid-round-trip-contract/test-tsconfig.json --pretty false` | Estágios da seção 4 |
| Focal | `pnpm exec vitest run server/workflow-visualization-uuid-contract.test.ts server/sprint17-workflow-viz.test.ts server/tenant-g2-rule-read-callers.test.ts server/tenant-g2-override-log-callers.test.ts --pool=forks --maxWorkers=1 --minWorkers=1` | exit 0 |
| Regressão completa | `pnpm test --pool=forks --maxWorkers=4 --minWorkers=1` | exit 0, sem regressão contra 2.959 aprovados / 340 ignorados |
| Espaços em branco | `git diff --check` | exit 0 |

O laboratório PostgreSQL opt-in **não é exigido**: nenhum SQL muda. Se executado, é regressão, não prova desta unidade.

## 8. Fronteiras de commit

Dois commits locais **separados**, conforme a restrição 11 do workflow, **cada um dependente de autorização humana própria no momento de sua criação**:

1. **Commit de código:** ciclos A e B completos — `server/workflow-visualization-router.ts`, `client/src/pages/Workflow.tsx`, a nova suíte e as três suítes adaptadas. Nenhum documento.
2. **Commit documental:** exatamente estes três arquivos, e nenhum código:
   - `docs/security/workflow-visualization-uuid-round-trip-contract/2026-09-16-design.md`
   - `docs/security/workflow-visualization-uuid-round-trip-contract/2026-09-16-plan.md`
   - `docs/security/workflow-visualization-uuid-round-trip-contract/2026-09-16-evidence.md`

   O desenho e o plano ainda não estão versionados; entram neste commit junto da evidência.

Sem `amend`, `rebase`, push, PR, merge ou deploy. O `test-tsconfig.json` de evidência e os logs ficam **fora** dos commits, sob `.superpowers/`, que o `.gitignore` já exclui.

## 9. Rollback

Reverter o commit de código restaura exatamente o estado atual: nada é persistido, migrado ou convertido, e nenhum dado muda de forma. O commit documental pode ser mantido como registro histórico ou revertido separadamente. Não há janela operacional nem passo de migração a desfazer.

## 10. Exclusões explícitas

`learningLayer.getSuggestion` (`server/learning-layer-router.ts:225-228`) e a **fronteira** de `listDraftsForProject` (`protectedProcedure`, `:313`) estão **fora desta unidade** por decisão humana registrada. Também excluídos: schema, migrações, seeds, backfill e dados; `entityId` numérico de `issue-report-router.ts:32`; `Number(quantity/confidence)` do router; os 56 diagnósticos legados de `sprint17`; motores, preços e demais telas; helpers de histórico G2 já encerrados; PR #9; `.superpowers`; dependências, automação, CI e hooks.

## 11. Riscos e paradas obrigatórias

Riscos: falso verde por ausência de checagem de tipos nos testes, endereçado pela seção 4; RED B executado antes do GREEN A, que falharia pelo motivo errado; ciclo terminado com cliente ou suítes G2 incompatíveis, endereçado pela composição dos GREENs; quebra de contrato do retorno para consumidor não observado (nenhum além de `Workflow.tsx:514`); vazamento da abreviação para fora da apresentação, contido pelo invariante do GREEN B; ausência de harness de cliente, que mantém a aceitação restrita a caller tests e typecheck.

Parar e devolver ao humano se: qualquer RED não falhar pelo motivo esperado; um estágio de tipos não produzir os diagnósticos previstos; o contraste da seção 6 exigir enfraquecer o guard real; surgir necessidade de schema, migração, dado ou mudança de fronteira; aparecer consumidor do contrato numérico fora dos arquivos inventariados; ou a regressão completa divergir da base por motivo não explicado.

## 12. Não-claims

Este plano não prova correção, não concede GO de segurança, merge ou campo, e **não faz nenhuma alegação de funcionamento visual** — não existe harness de cliente neste repositório, e a verificação de carregamento no navegador permanece validação posterior explícita. **PR #9, segurança global e liberação de campo permanecem sem GO.** A execução depende de aprovação humana explícita, e cada commit depende da sua própria autorização.
